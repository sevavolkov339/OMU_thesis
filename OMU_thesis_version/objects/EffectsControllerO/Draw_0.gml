//var cam = view_camera[0];
//var vw = camera_get_view_width(cam);
//var vh = camera_get_view_height(cam);

//draw_set_color(c_white);

//for (var i = 0; i < array_length(lines); i++)
//{
//    var l = lines[i];

//    draw_set_alpha(l.alpha);
//    draw_line(l.x, l.y, l.x, l.y + l.len);
//}

//draw_set_alpha(1);